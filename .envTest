# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=usuario
DB_PASSWORD=clave
DB_NAME=medical_system
DB_SSLMODE=disable

# JWT Configuration
JWT_SECRET=token-generado-a-tu-gusto

# Server Configuration
PORT=8080